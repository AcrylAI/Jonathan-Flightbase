import { create } from '@storybook/theming';
import logo from './BI_Jonathan.svg';

export default create({
  base: 'light',
  brandTitle: 'Jonathan Platform',
  brandUrl: 'https://portal.acryl.ai/',
  brandImage: logo,
});
