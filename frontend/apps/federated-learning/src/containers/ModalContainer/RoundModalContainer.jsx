import RoundModal from '@src/components/pageContents/RoundPageContents/RoundModal';

function RoundModalContainer({ data, type }) {
  return <RoundModal data={data} type={type} />;
}

export default RoundModalContainer;
