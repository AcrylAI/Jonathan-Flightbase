import EditMemoModal from '@src/components/pageContents/ModelsPageContents/EditMemoModal';

function EditMemo({ data, type }) {
  return <EditMemoModal data={data} type={type} />;
}

export default EditMemo;
