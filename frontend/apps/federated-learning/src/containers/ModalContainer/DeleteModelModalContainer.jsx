import DeleteModelModal from '@src/components/pageContents/ModelsPageContents/DeleteModelModal';

function DeleteModelModalContainer({ data, type }) {
  return <DeleteModelModal data={data} type={type} />;
}

export default DeleteModelModalContainer;
