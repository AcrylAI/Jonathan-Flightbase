import DeleteClientModal from '@src/components/pageContents/ClientPageContents/DeleteClientModal';

function DeleteClientModalContainer({ data, type }) {
  return <DeleteClientModal data={data} type={type} />;
}

export default DeleteClientModalContainer;
