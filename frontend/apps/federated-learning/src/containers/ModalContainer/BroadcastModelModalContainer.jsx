import BroadcastModelModal from '@src/components/pageContents/ModelsPageContents/BroadcastModelModal';

function BroadcastModelModalContainer({ data, type }) {
  return <BroadcastModelModal data={data} type={type} />;
}

export default BroadcastModelModalContainer;
