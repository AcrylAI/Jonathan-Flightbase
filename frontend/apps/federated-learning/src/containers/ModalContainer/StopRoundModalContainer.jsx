import StopRoundModal from '@src/components/pageContents/RoundPageContents/StopRoundModal';

function StopRoundModalContainer({ data, type }) {
  return <StopRoundModal data={data} type={type} />;
}
export default StopRoundModalContainer;
